.DS_Store
.gradle
.idea
.intellijPlatform
.kotlin
.qodana
build

# Local to this machine, kept out of the repo
.mcp.json
phpstorm-agentic-plugin-brainstorm.md
/.env
# Plugin signing: certificate and private keys. Directory rule on purpose, so a key
# added later is covered without touching this file. Never commit anything from here.
/.sign/
